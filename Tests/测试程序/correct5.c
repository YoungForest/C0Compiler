const int MAX = 126, MIN = 32, gap = 34;

void putchar(char ch)
{
    printf(ch);
}
void func()
{
    int ch, j, k, i;
    i = 100;
    j = 40;
    k = 20;
    putchar('_');
    ch = '/';
    if( ch + 10 < MAX)
        if( ch + 10 != MIN)
            if (ch - 5 > MIN)
                if (ch - 4 >= MIN)
                    if (ch + 15 <= MAX)
                        if(ch + 10 != gap)
                            if (ch)
                                if (ch == 47)
                                    if (ch == '/')
                                        printf(ch - 10);
}

void main()
{
{}{    func();;;;;;;
{;
  ;;  return ;
}}}
